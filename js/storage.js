// Declare event object with elements and initialise it.

// let bookings = {
//   organizer: "Brvcase Inc.",
//   duration: 15,
//   description:
//     "You will be contacted Immmediately, once your appointment is confirmed",
//   date: new Date(),
//   time: "",
//   attendees: [],
//   name: "Brvcase Appointment",
// };

// localStorage.setItem("bookObj", JSON.stringify(bookings));

// export const addBookmark = function (recipe) {
//   // Add bookmark
//   state.bookmarks.push(recipe);

//   // Mark current recipe as bookmarked
//   if (recipe.id === state.recipe.id) state.recipe.bookmarked = true;

//   persistBookmarks();
// };

// const persistBookmarks = function () {
//   localStorage.setItem("bookmarks", JSON.stringify(state.bookmarks));
// };

// export const deleteBookmark = function (id) {
//   // Delete bookmark
//   const index = state.bookmarks.findIndex(el => el.id === id);
//   state.bookmarks.splice(index, 1);
